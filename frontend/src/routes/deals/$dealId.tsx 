import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/deals/$dealId')({
  component: () => (
    <div style={{ padding: '2rem' }}>
      <h1>✅ Dynamic route works!</h1>
      <p>This is the detail page for a specific deal.</p>
    </div>
  ),
})
